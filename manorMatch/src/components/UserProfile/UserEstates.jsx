/* eslint-disable react/prop-types */
const UserEstates = ({ currentUser }) => {

  return (
    <div>
      <h2 className="text-center my-4">Currently Servicing</h2>
      <div>
        {/* <p>{currentUser.address1}</p>
        <p>{currentUser.address2}</p>
        <p>{currentUser.city}, {currentUser.state}</p> */}
      </div>
    </div>
  );
}

export default UserEstates;